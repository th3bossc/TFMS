import 'package:flutter/material.dart';

import 'entities.dart';
class Transactions extends StatelessWidget {
  const Transactions({super.key,required Creds creds});

  @override
  Widget build(BuildContext context) {
    return Text("Trans");
  }
}
