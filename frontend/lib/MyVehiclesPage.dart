import 'package:flutter/material.dart';

import 'entities.dart';
class Vehicles extends StatelessWidget {
  const Vehicles({super.key,required Creds creds});

  @override
  Widget build(BuildContext context) {
    return Text("vehicles");
  }
}
